/**
 * mdx-components.js — requerido por @next/mdx con App Router
 * Define cómo se renderizan los elementos MDX en todo el proyecto.
 * Se pueden agregar componentes personalizados aquí en el futuro.
 */
export function useMDXComponents(components) {
  return {
    ...components,
  };
}
